
<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/Entry"><span class="fa fa-home"></span>&nbsp;Time Entry System</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
        <ul class="nav navbar-nav">            
            @if (User.IsInRole("Project Manager"))
            {
                <li><a href="/ValidateEntry"><span class="fa fa-users fa-fw"></span>&nbsp;Staff Entries</a></li>
            }
            @if (User.IsInRole("Timekeeper"))
            {
                <li><a href="/ValidateTime"><span class="fa fa-clock-o fa-fw"></span>&nbsp;Timekeeping</a></li>
            }            
        </ul>
        <ul class="nav navbar-nav navbar-right" style="margin-right:1px">
            <li><a href="#">Welcome, @ViewBag.FullName</a></li>
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="fa fa-cog"></span>&nbsp;Settings <b class="caret"></b></a>
                <ul class="dropdown-menu">                    
                    <li>@Html.ActionLink("Guidelines", "Index", "Guidelines")</li>
                    <li class="divider"></li>
                    <li>@Html.ActionLink("Logout", "Logout", "Login" )</li>
                </ul>
            </li>
        </ul>
    </div><!-- /.navbar-collapse -->
</nav>